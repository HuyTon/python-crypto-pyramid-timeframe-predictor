# Crypto Pyramid Timeframe Predictor (Streamlit)

A real-time(ish) trading dashboard for Binance Futures-style analysis.

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
```
